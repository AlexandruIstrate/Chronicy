# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ChronicyMacOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  platform :macos, '10.14'

  # Pods for ChronicyMacOS
  pod 'Alamofire'
end

target 'ChronicySafariExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  platform :macos, '10.14'

  # Pods for ChronicySafariExtension

end

target 'ChronicyShareExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  platform :macos, '10.14'

  # Pods for ChronicyShareExtension

end

target 'ChronicyiOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  platform :ios, '12.0'
  
  # Pods for ChronicyiOS
  pod 'Alamofire'
  pod 'Cards'
  pod 'BulletinBoard'
  
end
